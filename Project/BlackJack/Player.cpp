#include "Player.h"

#include <string>
using std::string;

string Player::showHand() const
{
    list<string>::const_iterator iter;
    for(iter = hand.begin(); iter != hand.end(); iter++) {

    }
}

