#include "Dealer.h"
#include <ctime>
int main()
{
    srand(std::time(0));
    Dealer dealer;

//    std::copy(vect.begin(), vect.end(), std::ostream_iterator<string>(std::cout, " "));
//    std::cout << "\n";
}
